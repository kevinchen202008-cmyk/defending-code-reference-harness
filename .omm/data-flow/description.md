追踪数据从目标源码到最终补丁的完整转换链路。关键中转节点：(1) PoC 字节是唯一跨越 Find→Grade 容器边界的数据（经宿主机中转——标注为 concern）；(2) found_bugs.jsonl 是由 Grade 写入、供 Find 读取的共享去重状态；(3) 每个工件在完成时立即落盘，无端对端批量缓冲；(4) ASAN 文本（非结构化数据）是主要信号，asan.py 从中提取结构化签名用于去重。
