harness/report.py。两代理阶段：Report 代理（200 轮，有工具）撰写 6 部分可利用性分析——原语（内存违规类型）、可达性（真实攻击面路径）、堆布局（触发稳定性）、提权路径（崩溃到 RCE/信息泄露）、约束条件（触发前提）、严重程度评级。随后 Report Grader（10 轮，无工具）对每节评分 0-2。可选：--novelty 标志在宿主机侧执行 git log 新颖性检查，注入 FIXED/UNFIXED 状态。
