七阶段执行管道：构建 → 侦察 → 发现 → 验证 → 裁决 → 报告 → 修复。每个阶段运行在独立的 Docker 容器中，工件（PoC 字节、diff）仅通过宿主机跨越阶段边界。并行性：N 个 Find 代理通过 asyncio.gather() 并发运行，轮询分配 focus_areas。Judge 和 Report 可在每次 grade 完成后立即串联（--stream 模式），使首个报告在首次崩溃后数分钟内出现，而无需等待所有运行结束。
